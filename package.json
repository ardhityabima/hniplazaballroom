{
  "name": "hni-plaza-ballroom",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'Static site - no build needed'",
    "deploy": "npx wrangler pages deploy public --project-name hni-plaza-ballroom"
  }
}
